---
phase: 22-core-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-feature/SKILL.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-03, CORE-04, CORE-05, CORE-06]

must_haves:
  truths:
    - "silver-feature/SKILL.md has explicit PATH 0, PATH 1, PATH 5, PATH 7, PATH 11, PATH 13 sections"
    - "Each path section has a prerequisite check on entry"
    - "Each path section has an exit condition verification"
    - "PATH 11 (VERIFY) is marked NON-SKIPPABLE"
    - "PATH 7 (EXECUTE) preserves GSD as sole execution engine"
    - "PATH 13 (SHIP) requires PATH 11 completed before running"
    - "Existing step logic is preserved — only restructured into path sections"
  artifacts:
    - path: "skills/silver-feature/SKILL.md"
      provides: "6 core composable path sections with prerequisite checks and exit conditions"
      contains: "## PATH 0: BOOTSTRAP"
  key_links:
    - from: "skills/silver-feature/SKILL.md PATH 0"
      to: "episodic-memory, gsd-new-project, gsd-map-codebase, gsd-new-milestone, gsd-resume-work, gsd-progress"
      via: "Skill tool invocations"
      pattern: "## PATH 0: BOOTSTRAP"
    - from: "skills/silver-feature/SKILL.md PATH 5"
      to: "gsd-discuss-phase, writing-plans, testing-strategy, gsd-plan-phase"
      via: "Skill tool invocations"
      pattern: "## PATH 5: PLAN"
---

<objective>
Restructure silver-feature/SKILL.md from a flat step pipeline into composable path sections implementing all 6 core paths (PATH 0 BOOTSTRAP, PATH 1 ORIENT, PATH 5 PLAN, PATH 7 EXECUTE, PATH 11 VERIFY, PATH 13 SHIP).

Purpose: Transform the primary feature workflow from a rigid sequential pipeline into prerequisite-checked, exit-condition-verified path units that the future /silver composer (Phase 25) can chain dynamically.

Output: Updated skills/silver-feature/SKILL.md with 6 core path sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/composable-paths-contracts.md
@docs/superpowers/specs/2026-04-14-composable-paths-design.md
@.planning/phases/22-core-paths/22-CONTEXT.md

<interfaces>
<!-- Path contract schema from docs/composable-paths-contracts.md -->
<!-- Each path section must contain: Prerequisites check, Trigger description, Steps (with Always/As-needed), Exit Condition check -->
<!-- Per D-01: paths are NOT separate files — they are sections within silver-* skill files -->
<!-- Per D-02: forward-compatible — works under current hook enforcement -->

From docs/composable-paths-contracts.md:
- PATH 0 BOOTSTRAP: prereq=None, exit=STATE.md exists with valid Current Position, review=ROADMAP.md+REQUIREMENTS.md through assessor
- PATH 1 ORIENT: prereq=PATH 0 completed (STATE.md exists), exit=Intel files exist or scan complete, review=None
- PATH 5 PLAN: prereq=ROADMAP.md+REQUIREMENTS.md+phase dir exist, exit=PLAN.md with plan-checker PASS (2 clean passes), review=CONTEXT.md+RESEARCH.md+PLAN.md through assessor
- PATH 7 EXECUTE: prereq=PLAN.md exists+STATE.md position matches phase, exit=All PLAN.md have SUMMARY.md+STATE.md advanced, review=failure inserts PATH 14
- PATH 11 VERIFY: prereq=PATH 7 completed (SUMMARY.md exists), exit=VERIFICATION.md with status:passed (2 clean passes), NON-SKIPPABLE, review=UAT.md through assessor
- PATH 13 SHIP: prereq=PATH 12 pre-ship passed+PATH 11 completed+clean tree+feature branch, exit=PR created+CI green, review=None

From skills/artifact-review-assessor/SKILL.md:
- Cycle: Reviewer -> Assessor -> fix MUST-FIX -> Reviewer (NOT Assessor again)
- No self-review rule on assessor
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure silver-feature/SKILL.md into composable path sections</name>
  <files>skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md
    - docs/composable-paths-contracts.md
    - docs/superpowers/specs/2026-04-14-composable-paths-design.md (§5 — PATH 0, 1, 5, 7, 11, 13)
    - .planning/phases/22-core-paths/22-CONTEXT.md
    - skills/artifact-review-assessor/SKILL.md
  </read_first>
  <action>
Rewrite skills/silver-feature/SKILL.md to replace the flat Step 0-17 pipeline with composable path sections. Preserve ALL existing logic — this is a restructure, not a rewrite of behavior.

**Structure the file as follows:**

1. Keep the existing frontmatter, title, description paragraph, Pre-flight, and Step-Skip Protocol sections unchanged.

2. Keep Step 0: Complexity Triage unchanged (this is pre-path routing, not a path itself).

3. Replace Steps 1a through 17 with these 6 path sections plus the non-core paths that remain as flat steps between them. The overall structure:

```
## PATH 0: BOOTSTRAP — Project/milestone lifecycle
### Prerequisite Check
(None — entry point)
### Steps
(Steps from contract: episodic-memory, gsd-new-project, gsd-map-codebase, gsd-new-milestone, gsd-resume-work, gsd-progress — all as-needed per contract)
### Review Cycle
ROADMAP.md and REQUIREMENTS.md through artifact-review-assessor per the Reviewer -> Assessor -> fix MUST-FIX -> Reviewer cycle until 2 consecutive clean passes.
### Exit Condition
STATE.md exists with valid Current Position. Verify: `grep -c "Current Position" .planning/STATE.md` returns 1.

## PATH 1: ORIENT — Codebase awareness
### Prerequisite Check
PATH 0 completed: STATE.md must exist. If not, STOP and run PATH 0.
### Steps
(gsd-intel always, gsd-scan as-needed, gsd-map-codebase as-needed — per contract)
### Exit Condition
Intel files exist in .planning/intel/ OR scan complete.

## Exploration & Ideation (Steps 1b-1d, 2-3)
(Keep existing Steps 1b Fuzzy Scope, 1c Brainstorm, 1d MultAI, Step 2 Testing Strategy, Step 2.5 Writing Plans, Step 2.7 Pre-Build Validation, Step 3 Quality Gates, Step 4-5 as they are — these map to PATH 2/3/4/6/12 which are implemented in Phase 23-24. Label them as "Non-core path steps — will become PATH 2, 3, 4, 12 in future phases")

## PATH 5: PLAN — Execution planning
### Prerequisite Check
ROADMAP.md exists, REQUIREMENTS.md exists, phase directory exists. Verify all three or STOP with specific missing-artifact message.
### Steps
1. gsd-discuss-phase (Always)
2. superpowers:writing-plans (Always — spec-to-plan bridge)
3. engineering:testing-strategy (Always)
4. gsd-list-phase-assumptions (As-needed)
5. gsd-analyze-dependencies (Always)
6. gsd-plan-phase (Always)
### Review Cycle
CONTEXT.md → review-context → artifact-review-assessor → fix MUST-FIX → review-context (2 consecutive clean passes).
RESEARCH.md → review-research → artifact-review-assessor → fix MUST-FIX → review-research (2 consecutive clean passes).
PLAN.md → gsd-plan-checker → artifact-review-assessor → fix MUST-FIX → gsd-plan-checker (2 consecutive clean passes, max 3 iterations).
### Exit Condition
PLAN.md exists with plan-checker PASS (2 consecutive clean passes).

## PATH 7: EXECUTE — GSD wave-based implementation
### Prerequisite Check
PLAN.md exists AND STATE.md position matches current phase. If either fails, STOP.
### Steps
1. superpowers:test-driven-development (As-needed — implementation plans only, per D-09)
2. gsd-execute-phase (Interactive mode) OR gsd-autonomous (Autonomous mode per §10e) — sole execution engine (per D-09)
3. context7-plugin:context7-mcp (Ambient — available during execution, per D-11)
### Failure Path
If execution fails mid-wave, do NOT mark phase complete. Route to silver:bugfix for triage. (PATH 14 DEBUG dynamic insertion implemented in Phase 24, per D-10.)
### Exit Condition
All PLAN.md files have SUMMARY.md, STATE.md advanced.

## Review, Security, Quality (Steps 9a-13)
(Keep existing Steps 9a-13 as they are — these map to PATH 9, 10, 12 pre-ship which are implemented in Phase 23-24. Label as "Non-core path steps — will become PATH 9, 10, 12 in future phases")

## PATH 11: VERIFY — Non-skippable verification
### Prerequisite Check
PATH 7 completed: at least one SUMMARY.md exists for current phase. If not, STOP.
### NON-SKIPPABLE
This path cannot be skipped regardless of §10 preferences. Refuse skip requests.
### Steps
1. gsd-verify-work (Always — NON-SKIPPABLE, per D-12)
2. gsd-add-tests (As-needed — coverage gaps, per D-12)
3. superpowers:verification-before-completion (Always, per D-12)
### Review Cycle
UAT.md → review-uat → artifact-review-assessor → fix MUST-FIX → review-uat (2 consecutive clean passes).
### Exit Condition
VERIFICATION.md with status: passed (2 consecutive clean passes, per D-13).

## PATH 13: SHIP — Deliver
### Prerequisite Check
PATH 12 pre-ship passed (quality gates step 13 above), PATH 11 completed (VERIFICATION.md exists with status: passed), clean working tree, on feature branch. If any fail, STOP with specific message. Per D-14.
### Steps
1. superpowers:finishing-a-development-branch (Always — merge/PR/cleanup decision)
2. gsd-pr-branch (As-needed — user choice, per D-14)
3. engineering:deploy-checklist (As-needed — production deployment)
4. gsd-ship (Always, per D-14)
### Exit Condition
PR created, CI green (per D-15).

## Episodic Memory (Step 16)
(Keep existing Step 16 unchanged)

## Milestone Completion (Step 17)
(Keep existing Step 17 unchanged — this maps to PATH 17 RELEASE, implemented in Phase 24)
```

**Key rules for the restructure:**
- Per D-01: paths are sections within this file, not separate files
- Per D-02: forward-compatible with current hooks, no hook changes
- Per D-03: contracts from docs/composable-paths-contracts.md are the reference
- Preserve the Step-Skip Protocol and non-skippable gates
- Each PATH section must have: Prerequisite Check, Steps, Exit Condition
- PATH 0 and PATH 5 also have Review Cycle subsections
- Non-core steps (exploration, ideation, review, security, quality gates) remain as flat steps between path sections with labels indicating future path assignment
- The file must still be a valid SKILL.md that Claude can follow as an orchestration guide
  </action>
  <acceptance_criteria>
    - grep -c "## PATH 0: BOOTSTRAP" skills/silver-feature/SKILL.md returns 1
    - grep -c "## PATH 1: ORIENT" skills/silver-feature/SKILL.md returns 1
    - grep -c "## PATH 5: PLAN" skills/silver-feature/SKILL.md returns 1
    - grep -c "## PATH 7: EXECUTE" skills/silver-feature/SKILL.md returns 1
    - grep -c "## PATH 11: VERIFY" skills/silver-feature/SKILL.md returns 1
    - grep -c "## PATH 13: SHIP" skills/silver-feature/SKILL.md returns 1
    - grep -c "### Prerequisite Check" skills/silver-feature/SKILL.md returns at least 6
    - grep -c "### Exit Condition" skills/silver-feature/SKILL.md returns at least 6
    - grep -c "NON-SKIPPABLE" skills/silver-feature/SKILL.md returns at least 2 (PATH 11 header + gate)
    - grep -c "artifact-review-assessor" skills/silver-feature/SKILL.md returns at least 3 (PATH 0, PATH 5, PATH 11 review cycles)
    - grep -c "gsd-execute-phase" skills/silver-feature/SKILL.md returns at least 1
    - grep -c "gsd-verify-work" skills/silver-feature/SKILL.md returns at least 1
    - grep -c "gsd-ship" skills/silver-feature/SKILL.md returns at least 1
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && grep -c "## PATH [0-9]*:" skills/silver-feature/SKILL.md | grep -q "6" && echo "PASS: 6 path sections" || echo "FAIL"</automated>
  </verify>
  <done>silver-feature/SKILL.md restructured with 6 core path sections, each having prerequisite check and exit condition, review cycles where applicable, NON-SKIPPABLE on PATH 11, all existing behavior preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file -> GSD engine | Skill invocations cross into GSD execution boundary |
| Path prerequisite checks | Artifact existence checks gate path entry |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | Tampering | PATH 11 skip | mitigate | NON-SKIPPABLE gate enforced in SKILL.md — refuse skip requests regardless of §10 preferences |
| T-22-02 | Elevation | PATH 13 without verification | mitigate | Prerequisite check requires VERIFICATION.md with status: passed before SHIP steps execute |
| T-22-03 | Spoofing | Fake VERIFICATION.md | accept | Low risk — single-user workflow, VERIFICATION.md produced by gsd-verify-work which has its own integrity checks |
</threat_model>

<verification>
- All 6 core path sections exist in silver-feature/SKILL.md
- Each path has prerequisite check and exit condition
- PATH 11 is non-skippable
- PATH 13 requires PATH 11 completion
- Review cycles reference artifact-review-assessor correctly
- Existing non-core steps preserved between path sections
</verification>

<success_criteria>
silver-feature/SKILL.md has 6 composable path sections (PATH 0, 1, 5, 7, 11, 13) with prerequisite checks, exit conditions, and review cycles matching the contracts from docs/composable-paths-contracts.md
</success_criteria>

<output>
After completion, create `.planning/phases/22-core-paths/22-01-SUMMARY.md`
</output>
