---
phase: 22-core-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-bugfix/SKILL.md
autonomous: true
requirements: [CORE-01, CORE-02, CORE-04, CORE-05, CORE-06]

must_haves:
  truths:
    - "silver-bugfix/SKILL.md has explicit PATH 1, PATH 7, PATH 11, PATH 13 sections"
    - "Each path section has a prerequisite check on entry"
    - "Each path section has an exit condition verification"
    - "PATH 11 (VERIFY) is marked NON-SKIPPABLE"
    - "PATH 7 (EXECUTE) preserves GSD as sole execution engine"
    - "Existing triage paths (1A/1B/1C) and TDD step preserved"
  artifacts:
    - path: "skills/silver-bugfix/SKILL.md"
      provides: "Core composable path sections for bugfix workflow"
      contains: "## PATH 7: EXECUTE"
  key_links:
    - from: "skills/silver-bugfix/SKILL.md PATH 11"
      to: "gsd-verify-work"
      via: "Skill tool invocation"
      pattern: "## PATH 11: VERIFY"
---

<objective>
Restructure silver-bugfix/SKILL.md to use composable path sections for the core paths it invokes (PATH 1 ORIENT, PATH 7 EXECUTE, PATH 11 VERIFY, PATH 13 SHIP). Bugfix does not use PATH 0 (BOOTSTRAP) or PATH 5 (PLAN) in full form — it has its own triage and lightweight planning.

Purpose: Align the bugfix workflow with the composable paths architecture while preserving its unique triage-first discipline.

Output: Updated skills/silver-bugfix/SKILL.md with core path sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/composable-paths-contracts.md
@.planning/phases/22-core-paths/22-CONTEXT.md

<interfaces>
<!-- Bugfix workflow maps to: triage (custom) -> PATH 14 DEBUG (Phase 24) -> TDD -> PATH 5 PLAN (lightweight) -> PATH 7 EXECUTE -> PATH 9 REVIEW (Phase 24) -> PATH 11 VERIFY -> PATH 10 SECURE (Phase 24) -> PATH 12 QUALITY GATE (Phase 24) -> PATH 13 SHIP -->
<!-- For Phase 22, we restructure the core paths: PATH 7, PATH 11, PATH 13 -->
<!-- PATH 1 ORIENT is implicit in bugfix (gsd-intel not currently called but should be available) -->
<!-- Triage paths 1A/1B/1C map to PATH 14 DEBUG (Phase 24) — keep as-is for now -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure silver-bugfix/SKILL.md with composable path sections</name>
  <files>skills/silver-bugfix/SKILL.md</files>
  <read_first>
    - skills/silver-bugfix/SKILL.md
    - docs/composable-paths-contracts.md
    - .planning/phases/22-core-paths/22-CONTEXT.md
    - skills/silver-feature/SKILL.md (read after Plan 01 executes for pattern consistency, but do not block on it)
  </read_first>
  <action>
Rewrite skills/silver-bugfix/SKILL.md to use composable path sections for the core paths. Preserve ALL existing behavior — this is a restructure.

**Structure the file as follows:**

1. Keep frontmatter, title, description, Pre-flight, Step-Skip Protocol, and Step 0: Triage unchanged.

2. Keep Path 1A, 1B, 1C (triage investigation paths) unchanged. Add a label: "Debug investigation paths — will align with PATH 14 (DEBUG) in Phase 24"

3. Keep Step 2: TDD unchanged (this is pre-path, unique to bugfix workflow).

4. Replace Steps 3-8 with composable path sections:

```
## PATH 5: PLAN — Fix planning (lightweight)
### Prerequisite Check
TDD regression test exists and is red (from Step 2). If not, STOP — go back to Step 2.
### Steps
1. gsd-plan-phase (Always — lightweight, 1-2 tasks only)
### Exit Condition
PLAN.md exists for the fix.
Note: Bugfix uses a lightweight version of PATH 5 — no discuss-phase, writing-plans, or testing-strategy since the bug investigation already established context and the TDD step already defined the test.

## PATH 7: EXECUTE — Fix implementation
### Prerequisite Check
PLAN.md exists for the fix AND regression test from Step 2 is red. If PLAN.md missing, STOP.
### Steps
1. gsd-execute-phase (Always — sole execution engine, per D-09)
2. Verify regression test from Step 2 is now GREEN after execution.
### Failure Path
If execution fails, route to Path 1A (systematic debugging) with execution failure context.
### Exit Condition
All PLAN.md tasks have SUMMARY.md, regression test is green.

## Review & Security (Steps 5, 7, 7b)
(Keep existing Steps 5 Code Review, 7 Security Review, 7b Quality Gates as they are — these map to PATH 9, 10, 12 which are implemented in Phase 24. Label as "Non-core path steps — will become PATH 9, 10, 12 in future phases")

## PATH 11: VERIFY — Non-skippable verification
### Prerequisite Check
PATH 7 completed: SUMMARY.md exists for current fix. Regression test is green. If not, STOP.
### NON-SKIPPABLE
This path cannot be skipped regardless of §10 preferences. Refuse skip requests.
### Steps
1. gsd-verify-work (Always — NON-SKIPPABLE, per D-12). Confirm fix, zero regression.
2. gsd-add-tests (As-needed — if coverage gaps beyond the regression test)
3. superpowers:verification-before-completion (Always)
### Exit Condition
VERIFICATION.md with status: passed (2 consecutive clean passes, per D-13).

## PATH 13: SHIP — Deliver fix
### Prerequisite Check
PATH 11 completed (VERIFICATION.md exists with status: passed), clean working tree, on feature branch. Quality gates (Step 7b) passed. If any fail, STOP.
### Steps
1. gsd-ship (Always — push branch, create PR, per D-14/D-15)
### Exit Condition
PR created, CI green (per D-15).
```

**Key rules:**
- Per D-01: paths are sections within this file, not separate files
- Per D-02: forward-compatible with current hooks
- Bugfix PATH 5 is deliberately lightweight — just gsd-plan-phase, no discuss/writing-plans/testing-strategy
- Triage paths (1A/1B/1C) remain as-is — they map to PATH 14 (Phase 24)
- TDD step (Step 2) is unique to bugfix — it stays before PATH 5
- Review/security/quality gate steps remain as flat steps between PATH 7 and PATH 11
  </action>
  <acceptance_criteria>
    - grep -c "## PATH 5: PLAN" skills/silver-bugfix/SKILL.md returns 1
    - grep -c "## PATH 7: EXECUTE" skills/silver-bugfix/SKILL.md returns 1
    - grep -c "## PATH 11: VERIFY" skills/silver-bugfix/SKILL.md returns 1
    - grep -c "## PATH 13: SHIP" skills/silver-bugfix/SKILL.md returns 1
    - grep -c "### Prerequisite Check" skills/silver-bugfix/SKILL.md returns at least 4
    - grep -c "### Exit Condition" skills/silver-bugfix/SKILL.md returns at least 4
    - grep -c "NON-SKIPPABLE" skills/silver-bugfix/SKILL.md returns at least 2
    - grep -c "gsd-execute-phase" skills/silver-bugfix/SKILL.md returns at least 1
    - grep -c "gsd-verify-work" skills/silver-bugfix/SKILL.md returns at least 1
    - grep -c "gsd-ship" skills/silver-bugfix/SKILL.md returns at least 1
    - grep -c "Path 1A\|Path 1B\|Path 1C" skills/silver-bugfix/SKILL.md returns at least 3 (triage preserved)
  </acceptance_criteria>
  <verify>
    <automated>cd /Users/shafqat/Documents/Projects/silver-bullet && grep -c "## PATH [0-9]*:" skills/silver-bugfix/SKILL.md | grep -q "4" && echo "PASS: 4 path sections" || echo "FAIL"</automated>
  </verify>
  <done>silver-bugfix/SKILL.md restructured with 4 core path sections (PATH 5, 7, 11, 13), each having prerequisite check and exit condition, NON-SKIPPABLE on PATH 11, triage paths preserved, TDD step preserved</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file -> GSD engine | Skill invocations cross into GSD execution boundary |
| Triage -> Debug paths | User selection routes to different investigation strategies |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-04 | Tampering | PATH 11 skip in bugfix | mitigate | NON-SKIPPABLE gate enforced — refuse skip requests regardless of §10 |
| T-22-05 | Elevation | Ship without regression test green | mitigate | PATH 7 exit condition requires regression test green; PATH 13 prerequisite requires PATH 11 completed |
</threat_model>

<verification>
- All 4 core path sections exist in silver-bugfix/SKILL.md (PATH 5, 7, 11, 13)
- Each path has prerequisite check and exit condition
- PATH 11 is non-skippable
- Triage paths 1A/1B/1C preserved
- TDD Step 2 preserved before PATH 5
- Review/security steps preserved between paths
</verification>

<success_criteria>
silver-bugfix/SKILL.md has 4 composable path sections (PATH 5, 7, 11, 13) with prerequisite checks and exit conditions, NON-SKIPPABLE on PATH 11, existing triage and TDD behavior preserved
</success_criteria>

<output>
After completion, create `.planning/phases/22-core-paths/22-02-SUMMARY.md`
</output>
