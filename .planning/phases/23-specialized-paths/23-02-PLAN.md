---
phase: 23-specialized-paths
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-ui/SKILL.md
  - skills/silver-release/SKILL.md
autonomous: true
requirements:
  - SPEC-04
  - SPEC-05
  - SPEC-06

must_haves:
  truths:
    - "silver-ui has PATH 6 (DESIGN CONTRACT) as a core step that is always active"
    - "silver-ui has PATH 8 (UI QUALITY) as a core step that is always active"
    - "silver-release has PATH 15 (DESIGN HANDOFF) between milestone audit and gap closure"
    - "PATH 15 triggers only when milestone has UI phases"
  artifacts:
    - path: "skills/silver-ui/SKILL.md"
      provides: "PATH 6 and PATH 8 sections for UI workflow"
      contains: "## PATH 6: DESIGN CONTRACT"
    - path: "skills/silver-release/SKILL.md"
      provides: "PATH 15 section for release workflow"
      contains: "## PATH 15: DESIGN HANDOFF"
  key_links:
    - from: "silver-release PATH 15"
      to: "Step 2 (milestone audit) and Step 2b (gap closure)"
      via: "inserted between audit and gap closure in release flow"
      pattern: "## PATH 15: DESIGN HANDOFF"
---

<objective>
Add PATH 6 (DESIGN CONTRACT) and PATH 8 (UI QUALITY) to silver-ui/SKILL.md, and PATH 15 (DESIGN HANDOFF) to silver-release/SKILL.md.

Purpose: silver-ui needs these UI paths as always-active core steps (not conditional like in silver-feature). silver-release needs PATH 15 to run design handoff inside the release flow for UI milestones.
Output: Updated silver-ui/SKILL.md and silver-release/SKILL.md.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-specialized-paths/23-CONTEXT.md
@docs/composable-paths-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add PATH 6 and PATH 8 to silver-ui/SKILL.md</name>
  <files>skills/silver-ui/SKILL.md</files>
  <read_first>
    - skills/silver-ui/SKILL.md (current state)
    - docs/composable-paths-contracts.md (PATH 6, 8 contracts)
    - .planning/phases/23-specialized-paths/23-CONTEXT.md (decisions D-14, D-15, D-17, D-18, D-19)
    - skills/silver-feature/SKILL.md (reference for PATH 6/8 pattern after Plan 01 updates — but this plan runs in parallel, so use the contracts doc as primary reference)
  </read_first>
  <action>
Restructure silver-ui/SKILL.md to use path sections for the UI-specific steps. Per D-14 and D-19, PATH 6 and PATH 8 are always active in silver-ui (no trigger condition needed — silver-ui is inherently UI work).

**Replace Step 5 (UI Phase — Design Contract) with PATH 6:**
- Heading: `## PATH 6: DESIGN CONTRACT — UI specification (iterative)`
- Prerequisite Check: PLAN.md exists for current phase. STOP if not met.
- Note: "Always active in silver-ui (UI workflow is inherently UI work — no trigger detection needed)."
- Steps (all via Skill tool):
  1. `design:design-system` (Always)
  2. `design:ux-copy` (As-needed — user-facing copy requires review)
  3. `gsd-ui-phase` (Always — produces UI-SPEC.md)
  4. `design:accessibility-review` (As-needed — WCAG 2.1 AA compliance check)
- Iterative: "User can loop steps 1-4. Claude suggests when design contract is solid; user decides exit."
- Exit Condition: UI-SPEC.md exists, user accepts design contract.

**Replace Step 9 (UI Visual Audit) with PATH 8:**
- Heading: `## PATH 8: UI QUALITY — Post-execution UI audit`
- Prerequisite Check: Execution complete, SUMMARY.md exists with UI deliverables. STOP if not met.
- Note: "Always active in silver-ui (no trigger detection needed)."
- Steps (all via Skill tool):
  1. `design:design-critique` (Always)
  2. `gsd-ui-review` (Always — 6-pillar audit: layout fidelity, accessibility, responsiveness, interaction quality, visual consistency, performance)
  3. `design:accessibility-review` (Always)
- Produces: UI-REVIEW.md. Fixes route through `gsd-execute-phase --gaps-only`.
- Review Cycle: UI-REVIEW.md through artifact-review-assessor, fix critical via GSD, re-audit.
- Exit Condition: UI-REVIEW.md exists with no critical findings, or user accepts.

Keep all other steps (0-4, 6-8 numbered steps, 10-16) as they are. Only replace Step 5 and Step 9 with path sections. Renumber the surrounding steps if needed to maintain logical flow (Step 5 becomes PATH 6 section, the existing Step 6 "Plan Phase" stays, Step 7 "Execute" stays, Step 8 "Code Review" stays, Step 9 becomes PATH 8 section, Step 10+ stays).
  </action>
  <verify>
    <automated>grep -c "## PATH 6: DESIGN CONTRACT" skills/silver-ui/SKILL.md && grep -c "## PATH 8: UI QUALITY" skills/silver-ui/SKILL.md && grep -c "design:design-critique" skills/silver-ui/SKILL.md && grep -c "gsd-ui-review" skills/silver-ui/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## PATH 6: DESIGN CONTRACT" skills/silver-ui/SKILL.md` returns a match
    - `grep "## PATH 8: UI QUALITY" skills/silver-ui/SKILL.md` returns a match
    - `grep "Always active in silver-ui" skills/silver-ui/SKILL.md` returns at least 2 matches
    - `grep "design:design-critique" skills/silver-ui/SKILL.md` returns a match
    - `grep "gsd-ui-review" skills/silver-ui/SKILL.md` returns a match
    - `grep "gsd-ui-phase" skills/silver-ui/SKILL.md` returns a match
    - `grep "UI-SPEC.md" skills/silver-ui/SKILL.md` returns a match
    - `grep "UI-REVIEW.md" skills/silver-ui/SKILL.md` returns a match
    - `grep "gaps-only" skills/silver-ui/SKILL.md` returns a match
    - The old "## Step 5: UI Phase" heading is replaced (no longer present as a flat step)
    - The old "## Step 9: UI Visual Audit" heading is replaced
  </acceptance_criteria>
  <done>silver-ui/SKILL.md has PATH 6 and PATH 8 as always-active path sections replacing the old flat steps. Both use the standard path section pattern with prerequisite check, steps, and exit condition.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATH 15 (DESIGN HANDOFF) to silver-release/SKILL.md</name>
  <files>skills/silver-release/SKILL.md</files>
  <read_first>
    - skills/silver-release/SKILL.md (current state)
    - docs/composable-paths-contracts.md (PATH 15 contract)
    - .planning/phases/23-specialized-paths/23-CONTEXT.md (decisions D-20, D-21, D-22)
  </read_first>
  <action>
Add PATH 15 (DESIGN HANDOFF) to silver-release/SKILL.md. Per D-20, it runs inside the release flow between Step 2 (milestone audit) and Step 2b (gap closure). Per D-21, it triggers only when the milestone has UI phases.

Insert a new section between Step 2a (Security Hard Gate) and Step 2b (Gap-Closure Loop):

**Heading:** `## PATH 15: DESIGN HANDOFF — Milestone UI handoff`

**Prerequisite Check:**
```bash
# Scan phase directories for UI-SPEC.md or UI-REVIEW.md existence
ls .planning/phases/*/UI-SPEC.md .planning/phases/*/UI-REVIEW.md 2>/dev/null | grep -q . || echo "SKIP: No UI phases in this milestone — PATH 15 not needed"
```

**Trigger note:** "Activated when milestone has UI phases (detected by UI-SPEC.md or UI-REVIEW.md in any phase directory) AND currently in release flow. Runs inside PATH 17 (RELEASE) only — never in the per-phase sequence."

**Steps** (all via Skill tool):
1. `design:design-handoff` (Always in this path — produce handoff package)
2. `design:design-system` (As-needed — final component inventory, design token reconciliation)

**Produces:** Handoff package.

**Exit Condition:** Handoff package produced.

Keep all existing steps and their numbering. PATH 15 is inserted as a new section between Step 2a and Step 2b, not replacing anything.
  </action>
  <verify>
    <automated>grep -c "## PATH 15: DESIGN HANDOFF" skills/silver-release/SKILL.md && grep -c "design:design-handoff" skills/silver-release/SKILL.md && grep -c "UI-SPEC.md" skills/silver-release/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## PATH 15: DESIGN HANDOFF" skills/silver-release/SKILL.md` returns a match
    - `grep "design:design-handoff" skills/silver-release/SKILL.md` returns a match
    - `grep "design:design-system" skills/silver-release/SKILL.md` returns a match (As-needed step)
    - `grep "UI-SPEC.md" skills/silver-release/SKILL.md` returns a match (trigger detection)
    - `grep "UI-REVIEW.md" skills/silver-release/SKILL.md` returns a match (trigger detection)
    - `grep "Handoff package" skills/silver-release/SKILL.md` returns a match (exit condition)
    - PATH 15 appears after Step 2a (Security Hard Gate) and before Step 2b (Gap-Closure Loop)
    - `grep "per-phase sequence" skills/silver-release/SKILL.md` returns a match (constraint documented)
  </acceptance_criteria>
  <done>silver-release/SKILL.md has PATH 15 (DESIGN HANDOFF) inserted between security gate and gap closure, with UI phase trigger detection and design handoff steps. PATH 15 runs inside PATH 17 only, never in per-phase sequence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file content | SKILL.md files are instructions for Claude — no user input crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-03 | T (Tampering) | SKILL.md files | accept | Version-controlled; no runtime user input processed |
| T-23-04 | E (Elevation) | PATH 15 trigger | mitigate | Explicit file existence check prevents PATH 15 from running on non-UI milestones |
</threat_model>

<verification>
1. `grep "## PATH 6: DESIGN CONTRACT" skills/silver-ui/SKILL.md` returns a match
2. `grep "## PATH 8: UI QUALITY" skills/silver-ui/SKILL.md` returns a match
3. `grep "## PATH 15: DESIGN HANDOFF" skills/silver-release/SKILL.md` returns a match
4. All three path sections have Prerequisite Check, Steps, and Exit Condition
</verification>

<success_criteria>
silver-ui/SKILL.md has PATH 6 and PATH 8 as always-active path sections. silver-release/SKILL.md has PATH 15 inserted in the release flow. All decisions D-14, D-15, D-19, D-20, D-21, D-22 implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/23-specialized-paths/23-02-SUMMARY.md`
</output>
