---
phase: 23-specialized-paths
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - skills/silver-feature/SKILL.md
autonomous: true
requirements:
  - SPEC-01
  - SPEC-02
  - SPEC-03
  - SPEC-04
  - SPEC-05

must_haves:
  truths:
    - "PATH 2 (EXPLORE) section exists with prerequisite check, 5 steps, trigger condition, and exit condition"
    - "PATH 3 (IDEATE) section exists with 4 steps and MultAI conditional sub-step"
    - "PATH 4 (SPECIFY) section exists with skip condition, 4 steps, and review cycles"
    - "PATH 6 (DESIGN CONTRACT) section exists between PATH 5 and PATH 7 with UI trigger detection"
    - "PATH 8 (UI QUALITY) section exists between PATH 7 and review steps with trigger from PATH 6 or UI file types"
  artifacts:
    - path: "skills/silver-feature/SKILL.md"
      provides: "All 5 specialized path sections for silver-feature"
      contains: "## PATH 2: EXPLORE"
  key_links:
    - from: "PATH 2"
      to: "Step 0 complexity triage"
      via: "fuzzy/complex classification routes to PATH 2"
      pattern: "PATH 1 completed"
    - from: "PATH 6"
      to: "PATH 5 and PATH 7"
      via: "inserted between PLAN and EXECUTE"
      pattern: "## PATH 6: DESIGN CONTRACT"
---

<objective>
Replace all [future] placeholder steps in silver-feature/SKILL.md with full composable path sections for PATH 2 (EXPLORE), PATH 3 (IDEATE), PATH 4 (SPECIFY), PATH 6 (DESIGN CONTRACT), and PATH 8 (UI QUALITY).

Purpose: These 5 specialized paths complete the silver-feature workflow, converting flat placeholder steps into structured path sections with prerequisite checks, triggers, steps, review cycles, and exit conditions.
Output: Updated skills/silver-feature/SKILL.md with all 5 path sections.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-specialized-paths/23-CONTEXT.md
@docs/composable-paths-contracts.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PATH 2, 3, 4 placeholder steps with full path sections</name>
  <files>skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (current state with [future] placeholders)
    - docs/composable-paths-contracts.md (PATH 2, 3, 4 contracts)
    - skills/silver-bugfix/SKILL.md (reference for established path section pattern from Phase 22)
    - .planning/phases/23-specialized-paths/23-CONTEXT.md (decisions D-01 through D-10)
  </read_first>
  <action>
Replace the entire `## Non-core path steps: Exploration & Ideation` section (from that heading through `### Step 3: Pre-Plan Quality Gates`) with three full path sections. Follow the exact pattern established in Phase 22 (see PATH 5, 7, 11, 13 in the same file for the pattern: heading, prerequisite check, steps list, review cycle if any, exit condition).

**PATH 2: EXPLORE** (replaces Step 1b) — per D-01, D-02, D-03:
- Heading: `## PATH 2: EXPLORE — Problem space understanding`
- Prerequisite Check: PATH 1 completed (intel files exist or scan complete). STOP if not met.
- Trigger note: "Activated when complexity triage (Step 0) classifies work as fuzzy or complex. Skipped for simple work."
- Steps (all via Skill tool):
  1. `gsd-explore` (Always)
  2. `product-management:product-brainstorming` (Always)
  3. `design:user-research` (As-needed — user-facing work)
  4. `product-management:synthesize-research` (As-needed — prior research exists)
  5. `product-management:competitive-brief` (As-needed — competitive landscape relevant)
- No review cycle.
- Exit Condition: Problem space clarified, scope boundaries established.

**PATH 3: IDEATE** (replaces Steps 1c and 1d) — per D-04, D-05, D-06:
- Heading: `## PATH 3: IDEATE — Convergent ideation + structural shaping`
- Prerequisite Check: PATH 2 completed. STOP if not met. Skip condition: skipped for simple/clear-scope work.
- Steps (all via Skill tool):
  1. `superpowers:brainstorming` (Always)
  2. `engineering:architecture` (As-needed — new service, cross-cutting concern, ADR-worthy)
  3. `engineering:system-design` (As-needed — new service boundary, major component)
  4. `design:design-system` (As-needed — UI phase, new component type)
- MultAI conditional sub-step (keep the existing Step 1d trigger logic and AskUserQuestion prompt, but nest it inside PATH 3 as a conditional sub-step after step 4).
- No review cycle.
- Exit Condition: Architectural direction chosen, design approach locked.

**PATH 4: SPECIFY** (replaces Steps 2, 2.5, 2.7) — per D-07, D-08, D-09, D-10:
- Heading: `## PATH 4: SPECIFY — Requirements convergence`
- Prerequisite Check: PATH 3 completed OR user has external spec to ingest. STOP if not met.
- Skip condition: "May be skipped ONLY when REQUIREMENTS.md already exists (from PATH 0). If REQUIREMENTS.md does not exist and PATH 4 is excluded, the composer MUST insert it."
- Steps (all via Skill tool):
  1. `silver-ingest` (As-needed — JIRA/Figma/Google Docs)
  2. `product-management:write-spec` (As-needed — scaffold)
  3. `silver-spec` (Always — Socratic elicitation)
  4. `silver-validate` (Always — gap analysis). If BLOCK findings: STOP, do not proceed. Display message and offer return to silver-spec or re-run silver-validate.
- Review Cycle: SPEC.md, REQUIREMENTS.md, DESIGN.md (if exists), INGESTION_MANIFEST.md (if ingest) — each through artifact-reviewer with appropriate --reviewer flag, then artifact-review-assessor, fix MUST-FIX, 2 consecutive clean passes.
- Exit Condition: SPEC.md + REQUIREMENTS.md exist, silver-validate shows zero BLOCK findings.

**IMPORTANT per D-10:** Testing strategy and writing-plans do NOT go in PATH 4. They stay in PATH 5 (PLAN) where Phase 22 already placed them.

Also remove the `## Non-core path steps: Exploration & Ideation` comment wrapper — these are now real path sections, not "non-core" placeholders.

Keep the `## PATH 5: PLAN` section exactly as it is (from Phase 22). The new PATH 4 section goes immediately before PATH 5.
  </action>
  <verify>
    <automated>grep -c "## PATH 2: EXPLORE" skills/silver-feature/SKILL.md && grep -c "## PATH 3: IDEATE" skills/silver-feature/SKILL.md && grep -c "## PATH 4: SPECIFY" skills/silver-feature/SKILL.md && ! grep -c "\[future\]" skills/silver-feature/SKILL.md | grep -v "^0$" || echo "No [future] markers remain — PASS"</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## PATH 2: EXPLORE" skills/silver-feature/SKILL.md` returns a match
    - `grep "## PATH 3: IDEATE" skills/silver-feature/SKILL.md` returns a match
    - `grep "## PATH 4: SPECIFY" skills/silver-feature/SKILL.md` returns a match
    - `grep -c "\[future\]" skills/silver-feature/SKILL.md` returns 0
    - `grep "gsd-explore" skills/silver-feature/SKILL.md` returns a match (PATH 2 step 1)
    - `grep "silver-spec" skills/silver-feature/SKILL.md` returns a match (PATH 4 step 3)
    - `grep "silver-validate" skills/silver-feature/SKILL.md` returns a match (PATH 4 step 4)
    - `grep "artifact-review-assessor" skills/silver-feature/SKILL.md` returns a match (PATH 4 review cycle)
    - `grep "MultAI" skills/silver-feature/SKILL.md` returns a match (PATH 3 conditional sub-step)
    - PATH 2 appears after PATH 1 and before PATH 3
    - PATH 4 appears before PATH 5
    - The `## Non-core path steps: Exploration & Ideation` comment/heading is removed
    - `grep "Step 2: Testing Strategy" skills/silver-feature/SKILL.md` returns NO match (moved to PATH 5 per D-10)
    - `grep "Step 2.5: Writing Plans" skills/silver-feature/SKILL.md` returns NO match
    - `grep "Step 2.7: Pre-Build Validation" skills/silver-feature/SKILL.md` returns NO match
  </acceptance_criteria>
  <done>PATH 2, 3, 4 are full path sections with prerequisite checks, steps, review cycles (PATH 4), and exit conditions. All [future] placeholders for these paths are removed. Testing strategy and writing-plans remain in PATH 5 per D-10.</done>
</task>

<task type="auto">
  <name>Task 2: Add PATH 6 and PATH 8 sections, remove remaining [future] placeholders</name>
  <files>skills/silver-feature/SKILL.md</files>
  <read_first>
    - skills/silver-feature/SKILL.md (after Task 1 modifications)
    - docs/composable-paths-contracts.md (PATH 6, 8 contracts)
    - .planning/phases/23-specialized-paths/23-CONTEXT.md (decisions D-11 through D-19)
  </read_first>
  <action>
Add two new path sections and clean up the remaining `## Non-core path steps: Review, Security, Quality` placeholder section.

**PATH 6: DESIGN CONTRACT** (entirely new, per D-11 through D-15):
- Insert between `## PATH 5: PLAN` and `## PATH 7: EXECUTE`
- Heading: `## PATH 6: DESIGN CONTRACT — UI specification (iterative)`
- Prerequisite Check: PATH 5 completed (PLAN.md exists). STOP if not met.
- Trigger note: "Activated when phase involves UI — detected by keywords in phase name/goal (UI, frontend, component, design, layout, animation, responsive), UI file types in existing code (.tsx, .jsx, .css, .scss, .vue, .svelte), or DESIGN.md existence. Skipped for non-UI phases."
- Steps (all via Skill tool):
  1. `design:design-system` (Always in this path)
  2. `design:ux-copy` (As-needed — user-facing copy requires review)
  3. `gsd-ui-phase` (Always in this path — produces UI-SPEC.md)
  4. `design:accessibility-review` (As-needed — WCAG 2.1 AA compliance check)
- Iterative: "User can loop steps 1-4. Claude suggests when design contract is solid; user decides exit."
- Exit Condition: UI-SPEC.md exists, user accepts design contract. Verify: `[ -f ".planning/phases/{phase}/UI-SPEC.md" ]`

**PATH 8: UI QUALITY** (entirely new, per D-16 through D-18):
- Insert between `## PATH 7: EXECUTE` and the review/security steps section
- Heading: `## PATH 8: UI QUALITY — Post-execution UI audit`
- Prerequisite Check: PATH 7 completed with UI deliverables. STOP if not met.
- Trigger note: "Activated when PATH 6 (DESIGN CONTRACT) was in the composition, OR SUMMARY.md contains UI file types (.tsx, .jsx, .css, .scss, .vue, .svelte). Skipped when no UI deliverables exist."
- Steps (all via Skill tool):
  1. `design:design-critique` (Always in this path)
  2. `gsd-ui-review` (Always in this path — 6-pillar audit: layout fidelity, accessibility, responsiveness, interaction quality, visual consistency, performance)
  3. `design:accessibility-review` (Always in this path)
- Produces: UI-REVIEW.md. Fixes route through `gsd-execute-phase --gaps-only`.
- Review Cycle: UI-REVIEW.md through artifact-review-assessor, fix critical findings via GSD, re-audit.
- Exit Condition: UI-REVIEW.md exists with no critical findings, or user accepts.

Also update the `## Non-core path steps: Review, Security, Quality` section:
- Remove the `<!-- These steps map to PATH 9 ... -->` comment
- Keep all the existing Step 9a-9d, Step 10-13 content but update the wrapper comment to: `<!-- Review, Security, and Quality Gate steps — will become PATH 9, 10, 12 in Phase 24 -->`
- Remove any remaining `[future]` markers from step headings in that section
  </action>
  <verify>
    <automated>grep -c "## PATH 6: DESIGN CONTRACT" skills/silver-feature/SKILL.md && grep -c "## PATH 8: UI QUALITY" skills/silver-feature/SKILL.md && grep -c "gsd-ui-phase" skills/silver-feature/SKILL.md && grep -c "gsd-ui-review" skills/silver-feature/SKILL.md && grep -c "design:design-critique" skills/silver-feature/SKILL.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "## PATH 6: DESIGN CONTRACT" skills/silver-feature/SKILL.md` returns a match
    - `grep "## PATH 8: UI QUALITY" skills/silver-feature/SKILL.md` returns a match
    - `grep "gsd-ui-phase" skills/silver-feature/SKILL.md` returns a match (PATH 6 step 3)
    - `grep "design:design-critique" skills/silver-feature/SKILL.md` returns a match (PATH 8 step 1)
    - `grep "gsd-ui-review" skills/silver-feature/SKILL.md` returns a match (PATH 8 step 2)
    - `grep "design:accessibility-review" skills/silver-feature/SKILL.md` returns at least 2 matches (PATH 6 and PATH 8)
    - `grep "UI-SPEC.md" skills/silver-feature/SKILL.md` returns a match (PATH 6 exit)
    - `grep "UI-REVIEW.md" skills/silver-feature/SKILL.md` returns a match (PATH 8 produces)
    - PATH 6 appears between PATH 5 and PATH 7 in the file
    - PATH 8 appears between PATH 7 and the review steps
    - `grep -c "\[future\]" skills/silver-feature/SKILL.md` returns 0
    - `grep "gaps-only" skills/silver-feature/SKILL.md` returns a match (PATH 8 fix routing)
  </acceptance_criteria>
  <done>PATH 6 (DESIGN CONTRACT) and PATH 8 (UI QUALITY) are full path sections inserted at correct positions. All [future] markers removed from silver-feature/SKILL.md. The file now has complete path sections for PATHs 0-8, 11, 13 with review/security steps preserved for Phase 24.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Skill file content | SKILL.md files are instructions for Claude — no user input crosses trust boundaries |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T (Tampering) | SKILL.md | accept | Skill files are version-controlled; no runtime user input processed |
| T-23-02 | E (Elevation) | PATH 4 skip condition | mitigate | Explicit guard: "May be skipped ONLY when REQUIREMENTS.md already exists" with verification check |
</threat_model>

<verification>
1. `grep -c "## PATH [0-9]" skills/silver-feature/SKILL.md` returns at least 8 (PATHs 0, 1, 2, 3, 4, 5, 6, 7, 8, 11, 13)
2. `grep -c "\[future\]" skills/silver-feature/SKILL.md` returns 0
3. All path sections have Prerequisite Check, Steps, and Exit Condition subsections
</verification>

<success_criteria>
silver-feature/SKILL.md contains full path sections for PATHs 2, 3, 4, 6, 8 with prerequisite checks, trigger conditions, numbered steps (via Skill tool), review cycles where applicable, and exit conditions. No [future] placeholders remain. All decisions D-01 through D-19 are implemented.
</success_criteria>

<output>
After completion, create `.planning/phases/23-specialized-paths/23-01-SUMMARY.md`
</output>
